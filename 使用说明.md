# AI-UK 提词器 · 使用说明

## 先说明一个限制

直接双击或通过隔空投送打开 `teleprompter.html`，页面本身可以使用，但它属于 `file://` 普通网页，不能注册 Service Worker，也不能作为真正的 PWA 添加到主屏幕。

如果要在 iPhone 上安装成全屏 PWA，必须把整个文件夹放到 **HTTPS 静态网址**，再用 Safari 打开这个网址。摄像头、麦克风和离线缓存也建议只通过 HTTPS 使用。

## Mac 端使用

1. 用 **Chrome 或 Firefox** 打开 `teleprompter.html`
2. 命令：按 `Cmd+O` → 选择文件，或直接拖入浏览器

## iPhone 端（PWA 小程序）

1. 用 Safari 打开部署后的 **HTTPS 网址**，确认页面可以正常加载
2. 点击底部工具栏的**分享按钮**
3. 向下滑动 → 选择**「添加到主屏幕」**
4. 主屏幕上出现「AI-UK 提词器」图标，点击即可全屏使用

## 操作

- 编辑页粘贴口播文案
- 纯提词模式：不上摄像头，仅练习跟随
- 提词+录制模式：摄像头预览 + 录制视频
- 录制中按空格暂停/继续，← → 微调速度，M 切换手动/跟随

## 快捷键

| 键 | 功能 |
|---|---|
| 空格 | 暂停 / 继续 |
| ← | 减速 |
| → | 加速 |
| M | 自动跟随 / 手动模式 |
| Esc / ✕ | 退出录制 |

## 离线

首次通过 HTTPS 加载后自动缓存，后续断网也能打开使用。直接打开本地文件不具备这个能力。
